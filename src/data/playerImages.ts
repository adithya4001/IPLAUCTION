// No player photo source is available: licensed cricket photography cannot be
// hotlinked reliably, and generated/cartoon avatars are not acceptable here.
// Cards fall back to the styled initials frame in PlayerPortrait.

export function getPlayerImageUrl(_playerName: string): string | undefined {
  return undefined;
}
